# Issue tracker: GitHub Issues

This project uses GitHub Issues for all work tracking.

## Creating issues

Use `gh issue create` or create directly in the GitHub UI.

## Reading issues

Skills read issues using the `gh` CLI. Ensure you have the GitHub CLI installed and authenticated.

## Label conventions

Labels are applied by the `triage` skill. See `triage-labels.md` for the vocabulary.
